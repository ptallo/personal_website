services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
        - .:/app
        - /app/node_modules
        - /app/dist
    environment:
      - VITE_ALLOWED_HOSTS=["localhost"]
    ports:
      - "8080:8080"